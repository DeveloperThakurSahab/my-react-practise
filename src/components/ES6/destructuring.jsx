import React from 'react';

const Destructuring = () => {
    return (
        <>
            <div className="container">
                <div className="row">
                    <div className="col">
                        <h2>Destructuring</h2>
                        <p>It makes it easy to extract <b>only what is needed</b>.<br/>
                            When destructuring arrays, the <b>order</b> that variables are declared is important.<br/>
                            The <b>object</b> properties do not have to be declared in a specific order.
                        </p>
                    </div>
                </div>
            </div>  
        </>
    );
};

export default Destructuring;